node_modules/
# pi install drops packages here; pi writes its own * .gitignore on first install,
# but we pre-ignore it so nothing is ever accidentally staged.
git/
# per-install user state — theme choice, flags, etc.
settings.json
# submodule build artifacts and runtime state

package-lock.json
*.db
*.db-wal
*.db-shm
.env
bin/rg
bin/fd
.claude/
.DS_Store
auth.json
sessions/

# Cleave child artifacts (written by Rust agent into worktrees)
.cleave-prompt.md
.cleave-session.json
npm/platform-packages/*/omegon-agent
.pi/memory/facts.jsonl
.devopet/memory/facts.jsonl

# graphify
graphify-out
